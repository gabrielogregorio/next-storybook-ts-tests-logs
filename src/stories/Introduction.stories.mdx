import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Getting started" />

# Introduction

Study of storybook

## Install

Install project using yarn commands

`yarn install`

## Run

for use project, run yarn commands

`yarn dev`

## Run tests

for test project, uses react testing library

`yarn test`

```typescript
type buttonType = {
  title: string;
};

export function Button({ title }: buttonType) {
  return <button>{title}</button>;
}
```
